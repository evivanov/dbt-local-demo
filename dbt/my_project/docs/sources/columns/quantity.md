{% docs source_col_quantity %}

The number of units sold in this transaction, stored as an INTEGER. This value represents the count of items purchased and should always be a positive number. When combined with price, this field enables calculation of total transaction value (price × quantity).

{% enddocs %}
